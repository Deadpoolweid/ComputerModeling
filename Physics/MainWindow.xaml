<Window 
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Physics"
        xmlns:Wpf="clr-namespace:OxyPlot.Wpf;assembly=OxyPlot.Wpf"
    x:Class="Physics.MainWindow"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Window.DataContext>
        <local:MainViewModel/>
    </Window.DataContext>
    <Grid>

        <!--<Wpf:Plot Title="{Binding Title}">
            <Wpf:Plot.Series>
                <Wpf:LineSeries ItemsSource="{Binding Points}"></Wpf:LineSeries>
            </Wpf:Plot.Series>
        </Wpf:Plot>-->
        <Wpf:PlotView Model="{Binding MyModel}" />
        
    </Grid>
</Window>
